package User;

import java.util.Map;

public class UserParser {

    public UserParser() {
        Map<String, User> userMap;
    }

    /*
    public static HashMap<String, User.User> userReaderToHash(File inputFile) {
        try (Scanner s = new Scanner(new FileInputStream(inputFile))) {
            while (s.hasNextLine()) {
                String[] userInfo = s.nextLine().split(",");

            }
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

     */
}
